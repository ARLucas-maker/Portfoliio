{
  "name": "portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Static portfolio site for arlucas.com.br - source of truth is /site, deploy folder is /dist",
  "type": "module",
  "scripts": {
    "build": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/sync.ps1",
    "preview": "powershell -NoProfile -Command \"Start-Process 'dist/index.html'\""
  }
}
